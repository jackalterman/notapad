# Notapad — Project State

**Last updated:** 2026-04-12 — B1, B2, B3, E2, E3, E5, E7, E8, E9, E10, E11, E13, E14, E15, E16, E17, E18, E21, E22, E23, E24 complete
**Phase:** Active Development

---

## Objectives (original)
1. Split `notapad.py` (~1700 lines) into a modular structure under `notapad_app/`.
2. Stabilize the custom menu system (`AntiqueMenu`) by converting it from a `tk.Toplevel` to an embedded `tk.Frame` using `.place()` — eliminating focus loss / windowing bugs on Windows.
3. Decouple dialogs, the code editor, syntax config, and main entry point.

---

## Module Structure
| File | Purpose |
|---|---|
| `notapad.py` | Main orchestrator: `Notapad` class, all UI wiring, file I/O, search logic, zoom, keybindings, `main()` entry point |
| `notapad_app/__init__.py` | Package init (minimal) |
| `notapad_app/config.py` | `APP_NAME`, `THEMES` dict (light/dark), syntax pattern dict `P`, `EXT_LANG`, `LANG_LABEL` |
| `notapad_app/dialogs.py` | `open_find`, `open_replace`, `goto_line`, `choose_font`, `show_about`, `show_appearance_submenu` |
| `notapad_app/editor.py` | `apply_highlight()` — viewport-scoped syntax highlighting via regex patterns from `config.P` |
| `notapad_app/settings.py` | `SettingsManager` — persistent user settings saved to `~/.notapad.json` |
| `notapad_app/ui_engine.py` | `AntiqueMenu` (embedded `tk.Frame` menu via `.place()`), `apply_windows_title_bar` (DWM dark mode) |
| `notapad.spec` | PyInstaller spec for standalone `.exe` build |
| `build/` | PyInstaller build artifacts |
| `dist/` | PyInstaller distributable output |

---

## Current Status
- **Refactor complete.** All planned modules exist and are fully wired.
- `notapad.py` is the lean orchestrator (~1191 lines after recent additions).
- A `settings.py` module was added organically (not in original plan) and is fully integrated.
- PyInstaller packaging is in place (`build/`, `dist/`, `notapad.spec`).
- A full code audit has been completed — see `.project-bugs.md` and `.project-enhancements.md`.

---

## Completed Tasks
- [x] Split monolith into `notapad_app/` package
- [x] `config.py`: themes, syntax patterns (`P`), `EXT_LANG`, `LANG_LABEL`
- [x] `ui_engine.py`: `AntiqueMenu` (tk.Frame + `.place()`) and `apply_windows_title_bar` — focus-loss bug fixed
- [x] `dialogs.py`: `open_find`, `open_replace`, `goto_line`, `choose_font`, `show_about`, `show_appearance_submenu`
- [x] `editor.py`: `apply_highlight` with viewport-scoped syntax highlighting (±40 lines)
- [x] `settings.py`: `SettingsManager` (added beyond original plan)
- [x] `notapad.py`: `Notapad` orchestrator class + `main()` entry point
- [x] PyInstaller packaging (`notapad.spec`, `build/`, `dist/`)
- [x] Full code audit completed (2026-04-12)
- [x] **B1 fixed** — `re` module shadow in `dialogs.py · open_replace()` — renamed Entry to `rep_ent`
- [x] **B2 fixed** — Replace All now brackets delete+insert with `edit_separator` — undo history preserved; added early-exit for zero-match case
- [x] **B3/E1 fixed** — `_schedule_highlight()` now properly debounces via `after(120ms)` with cancel — no more synchronous highlight on every keystroke
- [x] **E5 done** — `SettingsManager.set()` now debounces saves via `threading.Timer(2.0)` instead of writing to disk on every call; `save_immediate()` added for guaranteed flush on exit
- [x] **E8 done** — window geometry saved in `on_close()` and restored in `__init__` on next launch
- [x] **E7 done** — `last_file` and `last_cursor` persisted on close; `main()` restores them on launch unless a CLI path argument is given
- [x] **E11 done** — wrap-around indicator: `find_next`/`find_prev` detect wrap and flash "↩ Wrapped" in the match label for 1.2 s
- [x] **E14 done** — `status_lang` label is now clickable (`cursor=hand2`); opens an `AntiqueMenu` of all languages + Plain Text for manual override
- [x] **E15 done** — TypeScript gets its own syntax definition in `config.py`; `.ts`/`.tsx` remapped from `"javascript"` to `"typescript"` in `EXT_LANG`
- [x] **E10 done** — Ctrl+F now opens a Sublime-style inline find bar (slide-in `tk.Frame` at bottom of window, above status bar); Escape dismisses and returns focus to editor; old Find Toplevel remains available for Replace
- [x] **E17 done** — `open_file()` reads raw bytes to detect `\r\n`/`\n`/`\r`; `status_eol` label added to status bar; `save_file()` restores original line ending on write
- [x] **E21 done** — `<Return>` binding calls `_auto_indent()` which replicates leading whitespace of the current line
- [x] **E22 done** — `<Tab>` inserts `tab_size` spaces; `<Shift-Tab>` dedents; `tab_size` (default 4) and `use_spaces` (default True) added to `SettingsManager`; Format menu gains Tab Size submenu
- [x] **E16 done** — bracket matching: cursor adjacent to `([{` or `}])` highlights the paired bracket with `bracket_match` tag (underline + tint); idle-polled via `_schedule_bracket_match()` with 80 ms debounce
- [x] **E2 done** — `_do_search()` now caches `_match_positions` by `(term, flags, text_version)`; `_text_version` counter incremented on `<<Modified>>`; repeated F3/find_prev skips the full buffer scan when nothing has changed
- [x] **E3 done** — large file guard at 300k chars: `_redraw_gutter()` early-exits and `_schedule_highlight()` skips + updates `status_lang` with "⚡ Large file"; both reset automatically when content drops back below threshold
- [x] **E9 done** — `recent_files` list (max 10) added to `SettingsManager`; File menu gains a "Recent Files ▶" submenu rebuilt on every open/save; non-existent paths pruned on launch; `_push_recent()` helper manages the MRU list
- [x] **E13 done** — passive word highlight: when cursor rests on a word with no selection, all identical whole-word occurrences in the viewport are tagged `word_hi` (subtle bg tint); 400ms idle debounce via `_schedule_word_highlight()`; cleared on any keypress
- [x] **E18 done** — `open_file()` attempts `chardet` detection on non-UTF-8 bytes; falls back through latin-1 to replacement decoding; detected encoding shown in `status_enc` label; `save_file()` writes in the detected encoding; `new_file()` resets to UTF-8
- [x] **E23 done** — `<FocusIn>` on root checks `os.path.getmtime()` against `_file_mtime` saved at open/save; if changed, a non-blocking reload bar slides in above the find bar with Reload / Dismiss buttons
- [x] **E24 done** — `tkinterdnd2` drag-and-drop attempted at startup with graceful fallback; `<Drop>` event on root calls `open_file()` with the dropped path; works on Windows without any additional install when `tkinterdnd2` is present

---

## Next Steps (priority order)
1. **E4**  — Batch gutter redraws with single position pass (1 hr)
2. **E6**  — Incremental dirty-region highlighting (2 hrs)
3. **E12** — Replace within selection (1 hr)
4. **E19** — Chunked loading for files > 5 MB (3 hrs)

---

## Key Architecture Notes
- `AntiqueMenu` is an embedded `tk.Frame` using `.place()` on root — intentionally NOT a `Toplevel` — to prevent Windows focus/windowing bugs.
- `_on_app_click` in `Notapad` walks the widget hierarchy to detect clicks outside the menu and close it.
- `editor.py` highlights only the visible viewport ±40 lines for performance, with a 300,000-char hard cutoff.
- `settings.py` saves to `~/.notapad.json` with a 2-second debounce; `save_immediate()` is called on exit to guarantee flush.
- `SettingsManager` defaults: `theme_mode=system`, `word_wrap=True`, `show_line_nums=True`, `status_bar_visible=True`, `font_family=Consolas`, `font_size=11`, `geometry=None`, `last_file=None`, `last_cursor="1.0"`.
